import { ArticleCarousel, SafeAreaViewBackground } from "@/components";

const Home = () => {
  return (
    <SafeAreaViewBackground>
      <ArticleCarousel className="container mt-[32px]" />
    </SafeAreaViewBackground>
  );
};

export default Home;
